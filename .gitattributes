# Git LFS smoke-test assets
# Files under test-lfs/ with these extensions should be stored by Git LFS when committed through git-lfs.
test-lfs/*.bin filter=lfs diff=lfs merge=lfs -text
test-lfs/*.zip filter=lfs diff=lfs merge=lfs -text
